/**
 * Keep in sync with `--font-sans`, `--font-heading`, `--font-serif` in `src/styles.css`.
 * Used by Ant Design ConfigProvider so dashboards match the rest of the app.
 */
export const APP_FONT_FAMILY =
  '"Lexend", "Montserrat", "Cabin", ui-sans-serif, system-ui, sans-serif';

export const APP_FONT_FAMILY_HEADING =
  '"Montserrat", "Lexend", "Cabin", ui-sans-serif, system-ui, sans-serif';

export const APP_FONT_FAMILY_SERIF = '"Roboto Slab", ui-serif, Georgia, serif';
